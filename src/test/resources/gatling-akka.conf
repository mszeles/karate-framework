akka {
  actor {
    default-dispatcher {
      type = Dispatcher
      executor = "thread-pool-executor"
      thread-pool-executor {
        #Pool size should exceed the number of parallel users in test
        fixed-pool-size = 100
      }
      throughput = 1
    }
  }
}